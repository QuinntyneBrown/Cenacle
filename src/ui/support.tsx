import { Capability, CapabilityState, type CapabilityReport } from "../core/capabilities";
import { AppShell, Icon, LinkButton, StateView } from "./components";

export function SupportPage({ report }: { report: CapabilityReport | null }) {
  if (!report) return <AppShell><StateView mark="sage" title="Checking this browser" message="Cenacle is testing the capabilities on this device."/></AppShell>;
  return <AppShell><main className="container container-narrow section stack gap-5"><div><span className="eyebrow">Browser support</span><h1 className="h1 mt-3">{report.availableCount()} of 4 capabilities ready</h1><p className="lede mt-3">Each area degrades independently. Missing local capabilities are never replaced with a cloud service.</p></div><div className="support-grid">{Object.values(Capability).map((capability) => { const ready = report.stateOf(capability) === CapabilityState.Available; return <div className="card support-item" key={capability}><span className={`state__mark ${ready ? "state__mark--sage" : "state__mark--danger"}`}><Icon name={ready ? "shield" : "alert"}/></span><div><strong>{capability}</strong><p className="small muted">{ready ? "Ready" : "Not available"}</p></div></div>; })}</div>{!report.presenceReady() && <div className="banner banner--danger"><Icon name="alert"/><div><p className="banner__title">Live gathering unavailable here</p><p>WebTransport and WebCodecs are required. Use a current Chromium-based browser on a supported device, or copy this page link to one.</p><button className="btn btn--ghost btn--sm mt-3" onClick={() => void navigator.clipboard?.writeText(window.location.href)}><Icon name="copy"/>Copy link</button></div></div>}{!report.wordReady() && <div className="banner banner--warn"><p>On-device AI is unavailable, so captions, reflections, and Word actions are hidden. Presence can continue without a cloud fallback.</p></div>}{!report.visualsReady() && <div className="banner"><p>WebGPU is unavailable. Cenacle uses a still backdrop with zero requests.</p></div>}<LinkButton to="/" className="btn btn--primary">Return home</LinkButton></main></AppShell>;
}
